Design you database and let laravel-mmcr build a basic crud laravel project based on that design
It includes:
- Migrations
- Routes (api & web)
- Controllers with validators
- Models

> Just set database configuration in `config.php` and run as `php generate`
